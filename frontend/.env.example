# Base URL of the Autocompleter beta backend (no trailing slash, no /v1).
# For local backend development, override this with http://127.0.0.1:8000.
PUBLIC_BACKEND_BASE_URL=https://autocompleter-beta-backend.onrender.com
