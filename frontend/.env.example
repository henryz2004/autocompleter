# Base URL of the Autocompleter beta backend (no trailing slash).
# Must include the /v1 prefix, e.g.:
#   https://autocompleter-beta-backend.onrender.com
PUBLIC_BACKEND_BASE_URL=http://127.0.0.1:8000
